export enum GameTeamLocation {
  HOME = 'home',
  AWAY = 'away',
}

export enum GameScoreStatus {
  SCHEDULED = 'scheduled',
  FIRST_HALF = 'first_half',
  SECOND_HALF = 'second_half',
  HALF = 'half',
  FIRST_OVERTIME = 'first_over_time',
  SECOND_OVERTIME = 'second_over_time',
}
